{% extends "layouts/base.twig" %}

{% block body %}
    <div class="panel-wrapper panel-wrapper-centered">
        <div class="panel">
            {% include "_partials/alerts.twig" %}

            {{ block('panel') }}
        </div>

        <div class="mt-3 w-100 d-flex justify-content-center">
            {{ branding().color('black').render()|raw }}
        </div>
    </div>
{% endblock %}
